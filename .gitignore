# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# uv / venv
.venv/

# Build artifacts
dist/
build/
*.egg-info/

# Secrets — never commit App keys, tokens, or env files
*.pem
.env
.env.*

# Claude Code per-session local overrides — never committed (the tracked
# .claude/settings.json is the shared, fail-closed one)
.claude/settings.local.json

# macOS
.DS_Store
._*
